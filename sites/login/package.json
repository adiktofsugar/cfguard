{
    "name": "@sites/login",
    "version": "1.0.0",
    "description": "The login oidc provider",
    "main": "worker/index.ts",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "deploy": "vite build && wrangler deploy",
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "preview": "vite build --mode preview && node preview/server.mjs",
        "preview:dev": "concurrently \"vite build --mode preview --watch\" \"nodemon preview/server.mjs\""
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@picocss/pico": "^2.1.1",
        "@preact/signals": "^2.3.1",
        "express": "^4.21.2",
        "hono": "^4.9.5",
        "react-icons": "^5.5.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250831.0"
    }
}
